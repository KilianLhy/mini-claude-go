run:
  timeout: 3m

linters:
  enable:
    - gofmt
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - misspell
    - revive

issues:
  exclude-dirs:
    - bin
    - dist
